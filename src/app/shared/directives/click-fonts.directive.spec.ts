import { ClickFontsDirective } from './click-fonts.directive';

describe('ClickFontsDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickFontsDirective();
    expect(directive).toBeTruthy();
  });
});
